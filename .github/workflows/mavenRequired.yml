name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
    paths:
      - 'CONTRIBUTORS.md'
  pull_request:
    branches: [ "master" ]
    paths:
      - 'CONTRIBUTORS.md'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
